# Simple Yield Vault

A lightweight, flat-structure DeFi repository for staking ETH and tracking shares.

## Features
- Stake ETH to mint Vault Shares.
- Withdraw ETH by burning Shares.
- Zero-dependency logic (other than OpenZeppelin).
- "Flat" file structure for easy navigation.

## Setup
1. Install dependencies:
   ```bash
   npm install
